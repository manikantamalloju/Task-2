import { Box } from "@mui/system";
import React from "react";

function BarCharts() {
  return (
    <Box
      sx={{
        display: "flex",

        alignItems: "center",
        justifyContent: "center",
        height: "100vh",
        width: "100%",
      }}
    >
      BarCharts
    </Box>
  );
}

export default BarCharts;
// for responsive in cartsa
{/* <canvas style="margin: 0 auto;"></canvas> */}
